<%= link_to "Add Todo", new_todo_path %>

<ul class="todos">
  <% @todos.each do |t| %>
    <li class="<%= 'completed' if t.completed? %>">
      <%= t.title %>
      <%= button_to "Complete", todo_completion_path(t) %>
    </li>
  <% end %>
</ul>
